@model IEnumerable<webApipractica.Models.facultades>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre_facultad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_facultad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.facultad_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.facultad_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.facultad_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
